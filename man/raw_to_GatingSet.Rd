% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/raw_to_GatingSet.R
\name{raw_to_GatingSet}
\alias{raw_to_GatingSet}
\title{Create and Annotate GatingSet from Raw Data}
\usage{
raw_to_GatingSet(
  assayid,
  xml_path,
  fcs_path,
  sample_name = "Samples",
  xml_keywords = c("$FIL", "$TOT", "EXPERIMENT NAME", "Sample Order", "Stim",
    "Replicate")
)
}
\arguments{
\item{assayid}{Character. The unique ID for the assay (must match a value in `batchData$Batch`).}

\item{xml_path}{Character. Path to the FlowJo XML workspace file.}

\item{fcs_path}{Character. Path to the directory containing the FCS files.}

\item{sample_name}{Character. The name of the sample group/node in the XML to load. Default is "Samples".}

\item{xml_keywords}{Character vector. Keywords to extract from the XML workspace.}
}
\value{
Saves a GatingSet object to `data-raw/tmpdata/[assayid]` and returns the path invisibly.
}
\description{
This function parses a FlowJo XML workspace, associates it with raw FCS files,
and merges experimental metadata (thaw list/batch data) into the GatingSet's
phenotypic data (pData).
}
\details{
The function relies on a global or parent-environment object `batchData` which
must contain `Batch` and `SAMP_ORD` columns. The resulting GatingSet is saved
using `flowWorkspace::save_gs`.
}
